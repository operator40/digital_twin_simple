# --- API & keretrendszer ---
fastapi~=0.115.0
uvicorn[standard]~=0.30.0
pydantic~=2.8.0
pydantic-settings~=2.4.0

# --- Adatbázis réteg ---
SQLAlchemy~=2.0.35
asyncpg~=0.29.0
psycopg[binary]~=3.2.0

# --- HTTP kliens külső API-khoz ---
httpx~=0.27.0

# --- Observability / log (opcionális) ---
loguru~=0.7.2

# --- Migráció (opcionális) ---
alembic~=1.13.2

# --- Teszt (opcionális) ---
pytest~=8.3.0
pytest-asyncio~=0.23.6

pandas~=2.2